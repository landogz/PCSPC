/**
 * timekeeping module (SPA)
 * See config/navigation.php and docs/MODULES.md
 * Future: Axios + DataTables against /api/v1/timekeeping
 */
export function initTimekeepingModule() {
    // Scaffold only — feature APIs land with the mapped phase.
}