/**
 * administration module (SPA)
 * See config/navigation.php and docs/MODULES.md
 * Future: Axios + DataTables against /api/v1/administration
 */
export function initAdministrationModule() {
    // Scaffold only — feature APIs land with the mapped phase.
}