/**
 * overtime module (SPA)
 * See config/navigation.php and docs/MODULES.md
 * Future: Axios + DataTables against /api/v1/overtime
 */
export function initOvertimeModule() {
    // Scaffold only — feature APIs land with the mapped phase.
}