/**
 * performance module (SPA)
 * See config/navigation.php and docs/MODULES.md
 * Future: Axios + DataTables against /api/v1/performance
 */
export function initPerformanceModule() {
    // Scaffold only — feature APIs land with the mapped phase.
}