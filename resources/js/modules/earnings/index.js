/**
 * earnings module (SPA)
 * See config/navigation.php and docs/MODULES.md
 * Future: Axios + DataTables against /api/v1/earnings
 */
export function initEarningsModule() {
    // Scaffold only — feature APIs land with the mapped phase.
}