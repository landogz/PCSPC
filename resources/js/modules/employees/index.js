/**
 * employees module (SPA)
 * See config/navigation.php and docs/MODULES.md
 * Future: Axios + DataTables against /api/v1/employees
 */
export function initEmployeesModule() {
    // Scaffold only — feature APIs land with the mapped phase.
}